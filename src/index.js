/**
 * @author xiaojue[designsor@gmail.com]
 * @fileover lib index
 */
var combine = require('./lib/combine');
var server = require('./lib/server');

module.exports = {
  combine:combine,
  server:server
};
